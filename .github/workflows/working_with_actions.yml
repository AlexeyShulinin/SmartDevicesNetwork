name: Working with Actions

on:
  workflow_dispatch:
    
env:
  PROJECT_NAME: SmartDevicesNetwork.WebApi

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up dotnet via composite action
        uses: ./.github/actions/setup-dotnet
        with:
          dotnet-version: ${{ vars.DOTNET_VERSION }}
          project-name: ${{ env.PROJECT_NAME }}
          
      - name: Build Docker images
        run: |
          docker build -t smartdevicesnetwork-webapi:latest -f ${{ env.PROJECT_NAME }}/Dockerfile .
          docker save smartdevicesnetwork-webapi:latest -o webapi-image.tar

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: webapi-docker-image
          path: webapi-image.tar
      